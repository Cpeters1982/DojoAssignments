function incrementTheSeconds(arr){
    for (x = 0; x < array.length; x++){
        if (x % 2 == 1){
            array[x] = array[x] + 1;
        }
        console.log(array[x])
    }
    return arr;
}